buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath(androidTools.buildGradle)
        classpath(jetBrains.kotlinGradlePlugin)
        classpath(daggerHilt.gradle)
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}